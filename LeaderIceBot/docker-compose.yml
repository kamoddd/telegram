services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leadericebot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/bot_state.db
    volumes:
      - ./data:/app/data
